<header class="header">
  <div class="header-main">
    <nav class="nav">
      <a href="/" class="logo">Invexis</a>
      <ul class="nav-list">
        <li><a href="productos">PRODUCTOS</a></li>
        <li><a href="/precios">PRECIOS</a></li>
        <li><a href="/nosotros">NOSOTROS</a></li>
        <li><a href="/recursos">RECURSOS</a></li>
      </ul>
    </nav>
  </div>

  <!--btn inicio sesion y probar gratis -->
  <div class="btn-container">
    <div class="btn-box">
      <a href="/crear-cuenta" class="btn btn-primary">PROBAR GRATIS</a>
      <a href="/login" class="btn btn-secondary">INICIAR SESIÓN</a>
    </div>
  </div>

</header>

<style>

  .header {
    /* background-color: #212121; */ /* ELIMINADO - Movido a elementos internos */
    /* padding: 5px 10px; */ /* ELIMINADO - Header es solo contenedor */
    padding: 0; /* No se necesita padding para el contenedor */
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: sticky;
    z-index: 1000;
    top: 15px; /* Añadir espacio desde arriba */
    gap: 15px; /* Espacio entre contenido principal y botones */
    margin: 15px 25px; /* Añadir márgenes superior/inferior e izquierdo/derecho */
    /* border-radius: 50px; */ /* ELIMINADO - Redondeo en elementos internos */
    width: calc(100% - 50px); /* Ajustar ancho considerando márgenes izq/der */
    /* box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2); */ /* ELIMINADO - Movido a elementos internos */
  }

  .header-main {
    background-color: #212121; /* Tiene su propio fondo */
    padding: 5px 25px; /* Padding dentro de la barra principal */
    border-radius: 50px; /* Esquinas redondeadas */
    display: flex;
    align-items: center;
    flex-grow: 1; /* Permitir que ocupe el espacio disponible */
    /* box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2); */ /* Añadir sombra */
    min-height: 50px; /* Igualar altura de .btn-container */
  }

  .nav {
    display: flex;
    align-items: center;
    width: 100%;
    gap: 3rem; /* Ajustar espacio entre logo y lista de navegación */
  }

  .logo {
    font-size: 35px;
    text-decoration: none;
    color: #00ff51; /* Color original */
    font-family: "Arial Black", sans-serif; /* Sans-serif negrita, aproximación */
    font-weight: 900;
    white-space: nowrap; /* Evitar salto de línea */
  }

  .nav-list {
    display: flex;
    gap: 1.5rem; /* Espacio ajustado */
    list-style: none;
    margin: 0;
    padding: 0;
    margin-left: auto; /* Empujar enlaces a la derecha */
  }

  .nav-list a {
    /* color: white; */ /* Color original */
    color: #E0E0E0; /* Gris claro/blanco roto de la imagen */
    text-decoration: none;
    font-size: 14px; /* Ligeramente más pequeño */
    font-family: Arial, sans-serif; /* Sans-serif estándar */
    font-weight: 500; /* Peso regular */
    transition: color 0.2s ease;
    white-space: nowrap; /* Evitar salto de línea */
  }

  .nav-list a:hover {
    /* color: #00ff51; */ /* Hover original */
    color: white; /* Hover blanco simple */
  }

  /* Contenedor de botones */
  .btn-container {
      background-color: #212121; /* Tiene su propio fondo */
      padding: 5px 10px; /* Padding dentro del contenedor de botones */
      border-radius: 50px; /* Esquinas redondeadas */
      display: inline-flex; /* Ajustar al tamaño del contenido */
      align-items: center; /* Asegurar alineación vertical si la altura cambia */
      /* box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2); */ /* Añadir sombra */
      min-height: 50px; /* Forzar altura mínima */
  }

  .btn-box {
    display: flex;
    align-items: center;
    gap: 10px; /* Espacio entre botones */
  }

  .btn-box .btn {
    display: flex;
    justify-content: center;
    align-items: center;
    text-decoration: none;
    font-size: 14px; /* Igualar enlaces de nav */
    font-family: Arial, sans-serif; /* Sans-serif estándar */
    font-weight: 600; /* Ligeramente más negrita */
    border: none; /* Quitar borde original */
    border-radius: 50px; /* Botones completamente redondeados */
    padding: 10px 20px; /* Padding del botón */
    min-width: auto; /* Quitar ancho fijo */
    min-height: auto; /* Quitar altura fija */
    transform: none; /* Quitar inclinación */
    z-index: 1;
    overflow: hidden;
    transition: background-color 0.2s ease, color 0.2s ease; /* Transición suave */
    white-space: nowrap; /* Evitar salto de línea */
    cursor: pointer;
  }

  .btn-box .btn.btn-primary { /* Corresponde al estilo "PROBAR GRATIS" */
      background-color: #FF5A47; /* Fondo Rojo/Naranja */
      color: white;
  }

  .btn-box .btn.btn-primary:hover {
      background-color: #E54F3B; /* Tono más oscuro al pasar el ratón */
      color: white;
  }

  .btn-box .btn.btn-secondary { /* Corresponde al estilo "INICIAR SESIÓN" */
      background-color: #EAE5D9; /* Fondo beige */
      color: #333333; /* Texto oscuro */
  }

   .btn-box .btn.btn-secondary:hover {
      background-color: #DCD7CD; /* Beige más oscuro al pasar el ratón */
      color: #333333;
  }

  /* Quitar efectos de pseudo-elementos del estilo original */
  .btn-box .btn::before {
    display: none;
  }

</style>